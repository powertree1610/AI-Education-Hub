export default function ParentPortalPlaceholder() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-slate-500">Parent portal — coming in a later version.</p>
    </main>
  );
}
