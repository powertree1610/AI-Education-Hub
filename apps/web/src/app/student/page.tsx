export default function StudentPlaceholder() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-slate-500">
        Students use the kiosk — a teacher starts a session on a centre device.
      </p>
    </main>
  );
}
