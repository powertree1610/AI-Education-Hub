# Windows Server / IIS target: hoisted node_modules (no symlinks) so
# `next build` (standalone output tracing) works without the Windows
# symlink privilege (Developer Mode / admin).
node-linker=hoisted
